# Google Tag Manager container ID (looks like GTM-XXXXXXX).
# Find it in GTM under Admin > Container Settings, or on the "Install
# Google Tag Manager" screen when you first create the container.
# Leave unset locally — GTM is skipped entirely when this is empty.
PUBLIC_GTM_ID=
