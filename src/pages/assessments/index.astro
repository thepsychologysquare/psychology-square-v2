---
export const prerender = false;
// Assessments now live under /resources/assessments — this old URL
// redirects there so nothing that already linked to /assessments breaks.
return Astro.redirect('/resources/assessments', 301);
---
